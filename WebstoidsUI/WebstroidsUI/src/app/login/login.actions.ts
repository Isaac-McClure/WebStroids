import { createAction } from '@ngrx/store';

export const login = createAction('[Login Component] Login');
export const login_complete = createAction('[Login Component] Login');
export const login_error = createAction('[Login Component] Login');
